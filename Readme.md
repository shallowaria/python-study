10_000_000_000和10000000000是完全一样的
1.23x109就是1.23e9
同一个变量可以反复赋值，而且可以是不同类型的变量，这种变量本身类型不固定的语言称之为动态语言，与之对应的是静态语言。静态语言在定义变量时必须指定变量类型，如果赋值的时候类型不匹配，就会报错
/ 浮点数除
// 地板除 整数
% 取余
8比特 = 1字节 = 255(11111111)
2字节 = 65535
4字节 = 4294967295
Unicode 编码 = 2字节/生僻4字节
UTF-8 更进一步节约内存的 Unicode

占位符	替换内容
%d	    整数
%f	    浮点数
%s	    字符串
%x	    十六进制整数

只有1个元素的tuple定义时必须加一个逗号,，来消除歧义：t = (1,)
t = () 也是tuple

input()返回的数据类型是str